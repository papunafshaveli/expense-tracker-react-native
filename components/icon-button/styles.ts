import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  btnContainer: {
    borderRadius: 4,
    padding: 6,
    marginHorizontal: 8,
    marginVertical: 2,
  },
  pressed: {
    opacity: 0.8,
  },
});
